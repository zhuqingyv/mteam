# ExpandedView
胶囊展开态容器，承载 `ChatPanel`。当前内置 mock 消息与 agents 用于预览，无对外 Props。

## Props
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| — | — | — | 暂无对外 Props（内部使用 mock 数据） |

## Usage
```tsx
import ExpandedView from './ExpandedView';

<ExpandedView />
```
