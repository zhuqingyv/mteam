# DragHandle
窗口拖动条（视觉层），实际拖动在 Electron 侧处理。

## Props
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| visible | `boolean` | `true` | 是否显示 |

## Usage
```tsx
import DragHandle from './DragHandle';
<DragHandle visible />
```
