# MenuDots
六点菜单按钮，等价 `<Button variant="dots" />`。

## Props
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| onClick | `() => void` | - | 点击 |

## Usage
```tsx
import MenuDots from './MenuDots';
<MenuDots onClick={openMenu} />
```
