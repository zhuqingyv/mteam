# Logo
M-TEAM 徽标图片，离线自动灰度。

## Props
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| size | `number` | `56` | 尺寸 px |
| online | `boolean` | `true` | 在线状态（false 灰度+降透明） |

## Usage
```tsx
import Logo from './Logo';
<Logo size={56} online />
```
