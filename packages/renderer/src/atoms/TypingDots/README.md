# TypingDots
三个跳动圆点，表示输入中。

## Props
| Prop | Type | Default | Description |
|------|------|---------|-------------|
| color | `string` | - | 点颜色（默认 CSS 变量） |

## Usage
```tsx
import TypingDots from './TypingDots';
<TypingDots color="rgba(230,237,247,0.8)" />
```
