# SettingsPage
设置面板窗口入口：`PanelWindow` + `PrimaryAgentSettings` + `CliList`。
当前为骨架实现，数据待接 store/api。

## Entry
URL `?window=settings` 时渲染该页。

## Composes
- `templates/PanelWindow`
- `organisms/PrimaryAgentSettings`
- `molecules/CliList`
