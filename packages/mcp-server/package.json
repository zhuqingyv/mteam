{
  "name": "@mcp-team-hub/mcp-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "hub": "bun run src/hub.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "build:hub": "bun build src/hub.ts --outdir dist --target node",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.10.2",
    "uuid": "^11.1.0"
  },
  "bin": {
    "team-hub": "./src/cli.ts",
    "mcp-team-hub": "./src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "playwright": "^1.59.1",
    "typescript": "^6.0.2"
  }
}
