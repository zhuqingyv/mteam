{
  "name": "mcp-team-hub",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "start": "bun run --cwd packages/renderer start",
    "dev": "bun run start",
    "dev:backend": "bun run --cwd packages/backend src/http/server.ts",
    "dev:frontend": "bun run --cwd packages/renderer dev",
    "test": "bun test packages/backend/"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
