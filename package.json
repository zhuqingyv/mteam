{
  "name": "mcp-team-hub",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --cwd packages/mcp-server build",
    "dev": "bun run --cwd packages/mcp-server dev"
  }
}
